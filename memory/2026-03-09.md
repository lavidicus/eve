# Daily Memory - 2026-03-09

## Decisions
- Patched ocg ~/.openclaw/openclaw.json to set memory.backend -> "builtin" to satisfy CLI validation and bring gateway up.
- Chose quick fix approach to restore gateway availability on ocg; saved backups before edits.
- Sanitized workspace and session files on ocg to remove literal <tool_call> XML-like blocks to prevent gateway parse failures.
- Created Playbooks and first playbook entry (2026-03-03-tag-team-setup.md) and pushed to Eve repo.

## Blockers
- A config-management or process is reverting openclaw.json on ocg to an unsupported memory.backend value (sqlite-vec), causing restart loops.
- SSH access to dc is restricted for this assistant (Permission denied) preventing direct group send via dc's signal-cli; used alternative approaches where possible.
- Signal group ID mismatch between provided ID and the binding known to dc prevented sending to Workgroup Bots from dc without listing groups via the running daemon.

## Next steps / Action items
- Investigate source of config reversion on ocg (cron, CM tool, or agent) and remediate to prevent recurrence.
- Optionally implement a pre-send guard on ocg/dc to block literal "<tool_call>" outputs before they hit the parser (proposed change saved for review).
- If required, add ocg to allowlist or update bindings to include the intended Workgroup Bots group id on dc and/or grant SSH access to dc for direct operations.
- Monitor ocg gateway for N minutes to ensure it stays up; consider automated alerting if config flips again.

## CR / Issue tickets created
- No formal external CR/issue was opened; local backups created:
  - /home/localadmin/.openclaw/openclaw.json.bak (pre-patch)
  - /home/localadmin/.openclaw/openclaw.json.prepatch (pre-quick-fix)
  - Multiple .bak_YYYYMMDDThhmmssZ backups for files sanitized containing literal <tool_call> blocks.

## Configuration changes made (record)
- /home/localadmin/.openclaw/openclaw.json: changed memory.backend from "sqlite-vec" -> "builtin" (backup saved as openclaw.json.bak and openclaw.json.prepatch).
- Sanitized session/workspace files replacing literal XML-like tool tags with markers like [TOOL_CALL_REMOVED] and created timestamped .bak copies for each edited file.
- Ran `/home/localadmin/.npm-global/bin/openclaw doctor --fix` and restarted gateway on ocg; gateway now listening on 127.0.0.1:18789.

## Lessons learned / Technical notes
- Literal XML-like tool-call markers in workspace/session files cause gateway parser failures; sanitize artifacts and add guardrails to prevent reoccurrence.
- Running signal-cli concurrently (multiple JVM instances) can lock the config; query the running daemon via its HTTP API instead of spawning additional signal-cli processes.
- When fixing configs, always create timestamped backups before edits; these were saved and are available for audit.
- Gateway now validates config before restart (feature in newer releases) which helped avoid restart loops but also exposed the config drift issue.

## Project updates / State changes
- Sam (ocg): gateway restored and stable after quick fix; process running and RPC probe OK.
- Eve (dc): Playbooks and memory updates pushed to repo (commits: a5cee2e, 97c7f87). Signal pairing request pending for Sam (+13184061402).
- Sanitization completed on ocg workspace and sessions to remove malformed tool-call text; backups preserved.

---
Timestamp: 2026-03-09T18:34:00Z
Recorded by: Eve (assistant)
